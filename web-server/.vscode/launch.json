{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable 'web-server'",
      "cargo": {
        "args": ["build", "--bin=web-server", "--package=web-server"],
        "filter": {
          "name": "web-server",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
